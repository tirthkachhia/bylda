# ─── Supabase (public) ──────────────────────────────────────────────
# REQUIRES: Supabase Dashboard → Project Settings → API → anon public key
VITE_SUPABASE_URL="https://ipidfqwlszuhjgjygbvx.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID="ipidfqwlszuhjgjygbvx"

# ─── Stripe ─────────────────────────────────────────────────────────
# REQUIRES: Stripe Dashboard → Developers → API keys → Publishable key
# Use pk_test_... for sandbox, pk_live_... for production
VITE_PAYMENTS_CLIENT_TOKEN=""

# ─── Sentry (optional) ──────────────────────────────────────────────
# Client error tracking. Leave blank to disable (no-op). When set, errors are
# reported from src/lib/observability.ts.
VITE_SENTRY_DSN=""
VITE_SENTRY_ENVIRONMENT=""


# ─── Supabase server-side (Edge Functions & Cloudflare worker) ───────
# REQUIRES: Supabase Dashboard → Project Settings → API → service_role secret key
SUPABASE_SERVICE_ROLE_KEY=""
# REQUIRES: Supabase Dashboard → Project Settings → API → anon public key
SUPABASE_ANON_KEY=""
SUPABASE_URL="https://ipidfqwlszuhjgjygbvx.supabase.co"

# ─── Lovable AI Gateway ─────────────────────────────────────────────
# REQUIRES: Auto-provisioned by Lovable Cloud; visible in Lovable project secrets
LOVABLE_API_KEY=""

# ─── Stripe server-side (Edge Functions) ────────────────────────────
# REQUIRES: Stripe Dashboard → Developers → API keys → Secret key (sk_test_ / sk_live_)
STRIPE_SANDBOX_API_KEY=""
STRIPE_LIVE_API_KEY=""
# REQUIRES: Stripe Dashboard → Developers → Webhooks → Signing secret (whsec_...)
PAYMENTS_SANDBOX_WEBHOOK_SECRET=""
PAYMENTS_LIVE_WEBHOOK_SECRET=""

# ─── Integrations encryption ─────────────────────────────────────────
# REQUIRES: Generate with: openssl rand -base64 32
# Used server-side to encrypt delegated OAuth access and refresh tokens.
INTEGRATIONS_ENCRYPTION_KEY=""

# Call recording transcription. Set the same randomly generated secret in the
# Supabase project and the bylda-ai-api Cloudflare Worker.
TRANSCRIPTION_WORKER_URL="https://ai.usebylda.com/transcribe"
CALL_TRANSCRIPTION_SECRET=""

# ─── Connected-account OAuth apps (server-side only) ─────────────────────────
# Register this callback URL with every provider:
# https://ipidfqwlszuhjgjygbvx.supabase.co/functions/v1/integration-oauth-callback
APP_URL="https://app.usebylda.com"
HUBSPOT_CLIENT_ID=""
HUBSPOT_CLIENT_SECRET=""
GHL_CLIENT_ID=""
GHL_CLIENT_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
MICROSOFT_CLIENT_ID=""
MICROSOFT_CLIENT_SECRET=""
SLACK_CLIENT_ID=""
SLACK_CLIENT_SECRET=""
SALESFORCE_CLIENT_ID=""
SALESFORCE_CLIENT_SECRET=""
# Stripe Connect OAuth (separate from Bylda's subscription billing key)
STRIPE_CONNECT_CLIENT_ID=""
STRIPE_CONNECT_SECRET_KEY=""
MAILCHIMP_CLIENT_ID=""
MAILCHIMP_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
SHOPIFY_CLIENT_ID=""
SHOPIFY_CLIENT_SECRET=""
SHOPIFY_SCOPES="read_products,read_orders"
JIRA_CLIENT_ID=""
JIRA_CLIENT_SECRET=""
ASANA_CLIENT_ID=""
ASANA_CLIENT_SECRET=""
AIRTABLE_CLIENT_ID=""
AIRTABLE_CLIENT_SECRET=""
# PayPal Commerce Platform partner onboarding
PAYPAL_CLIENT_ID=""
PAYPAL_CLIENT_SECRET=""
PAYPAL_PARTNER_ID=""
PAYPAL_BN_CODE=""
PAYPAL_ENV="sandbox"
PAYPAL_PRODUCT="PPCP"
PIPEDRIVE_CLIENT_ID=""
PIPEDRIVE_CLIENT_SECRET=""
NOTION_CLIENT_ID=""
NOTION_CLIENT_SECRET=""
ZOOM_CLIENT_ID=""
ZOOM_CLIENT_SECRET=""
CALENDLY_CLIENT_ID=""
CALENDLY_CLIENT_SECRET=""
XERO_CLIENT_ID=""
XERO_CLIENT_SECRET=""

# ═══════════════════════════════════════════════════════════════════════
# ─── AI PROVIDER KEYS (Multi-model foundation) ───────────────────────
# Add each key in BOTH places:
#   1. Supabase Edge Functions  → supabase.com/dashboard → your project
#      → Edge Functions → Manage secrets → add secret
#   2. Cloudflare Workers       → wrangler secret put <NAME>
#      (or Cloudflare Dashboard → Workers → Settings → Variables → Secrets)
# ═══════════════════════════════════════════════════════════════════════

# ─── REQUIRED NOW (Phase 1) ──────────────────────────────────────────

# Anthropic (already in use — primary model)
# Get from: console.anthropic.com → API Keys
ANTHROPIC_API_KEY=""

# Cloudflare AI Gateway (already in use — routes Anthropic traffic)
# Get from: Cloudflare Dashboard → AI → AI Gateway → your gateway
CLOUDFLARE_ACCOUNT_ID=""
CLOUDFLARE_AI_GATEWAY_ID=""
# Build the URL as: https://gateway.ai.cloudflare.com/v1/{ACCOUNT_ID}/{GATEWAY_ID}
CLOUDFLARE_AI_GATEWAY_URL=""

# ─── PHASE 2: Add when ready to enable multi-provider routing ────────

# OpenAI — hot standby fallback for all Claude routes
# Get from: platform.openai.com → API keys → Create new secret key
OPENAI_API_KEY=""

# Grok (xAI) — real-time market data for competitor-scanner, gtm-strategy-builder
# Get from: console.x.ai → API Keys (requires xAI account)
GROK_API_KEY=""

# DeepSeek — cost-optimised batch tools (seo-audit, kpi-dashboard, email-sequence)
# Get from: platform.deepseek.com → API Keys
DEEPSEEK_API_KEY=""

# ─── PHASE 3: Enterprise / optional ──────────────────────────────────

# Perplexity — research mode with live web citations (operator only, no tool runs)
# Get from: www.perplexity.ai/settings/api → Generate
PERPLEXITY_API_KEY=""

# Google Gemini — future: PDF analysis, 1M-context sessions
# Get from: aistudio.google.com → Get API key
GEMINI_API_KEY=""

# OpenRouter — overflow, A/B model testing, exotic models
# Get from: openrouter.ai → Keys → Create Key
OPENROUTER_API_KEY=""
